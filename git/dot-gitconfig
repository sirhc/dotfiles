[advice]
    detachedHead = false

[alias]
    bd = for-each-ref --sort=committerdate --format='%(color:brightblack)%(objectname:short) %(color:green)%(committerdate:iso8601) %(color:blue)%(committerdate:relative) %(color:reset)%(refname:short)' refs/heads/
    br = branch -avv
    cached = diff --cached
    ci = commit
    co = checkout
    cod = ! git switch $( git default-branch )
    commits-per-day = ! git log --pretty=format:%cd --date=short | uniq -c
    current-branch = rev-parse --abbrev-ref HEAD
    dc = diff --cached
    db = config --local github.default-branch
    default-branch = config --local github.default-branch
    diff-default = ! git diff $( git config --local github.default-branch )..HEAD
    dt = difftool
    env = ! env | sort
    fa = fetch --all
    graph = log --all --graph --oneline --decorate
    hard = reset --hard HEAD
    hist = log --graph --pretty='[%h]%d %cd %s'
    lastlog = log -1 --pretty='%s'
    lg = log --graph --abbrev-commit --date=relative --date-order --format=format:'%C(yellow)%h%Creset -%C(yellow)%d%Creset %s %C(green)(%cr)%Creset %Cred<%an>%Creset'
    ls = ls-tree --abbrev=8 -r HEAD
    merge-default = ! git merge $( git default-branch )
    mrclean = clean -dfx  # Mr. Clean!
    mt = mergetool
    mylog = ! git log --author=`git config user.email` --stat -C
    one = log --oneline
    pr-url = ! env GH_PAGER= gh pr view --json url --jq '.url'
    root = rev-parse --show-toplevel
    set-default-branch = git config --local github.default-branch $( gh repo view --json defaultBranchRef --jq .defaultBranchRef.name )
    set-origin = ! git remote add origin $( gh repo view $( basename $( pwd ) ) --json url --jq .url )
    show-tags = ! git name-rev --tags --name-only $( git rev-parse HEAD )
    stashed = stash list --pretty=format:'%gd: %Cred%h%Creset %Cgreen[%ar]%Creset %s'
    stat = diff --stat
    tags = show-ref --tags
    unadd = restore --staged
    undo = ! "f() { git reset $( git rev-parse --abbrev-ref HEAD )@{${1:-1}}; }; f"
    wd = diff --word-diff
    whereis = branch --contains --remotes
    who = shortlog --numbered --summary --email --

[branch]
    sort = -committerdate

[color]
    ui = auto

[commit]
    gpgSign = true
    verbose = true

[core]
    editor = vim -i NONE
    excludesFile = ~/.gitignore
    pager = delta

[credential]
    helper = store

[delta]
    features = decorations
    hyperlinks = true
    hyperlinks-file-link-format = "file-line://{path}:{line}"
    light = false
    line-numbers = true
    navigate = true # use n and N to move between diff sections
    paging = always
    side-by-side = true

[delta "interactive"]
    keep-plus-minus-markers = false

[delta "decorations"]
    commit-decoration-style = blue ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = blue box
    hunk-header-file-style = red
    hunk-header-line-number-style = "#067a00"
    hunk-header-style = file line-number syntax

[diff]
    colorMoved = default
    renames = true
    tool = vimdiff
    wsErrorHighlight = all

[diff "daff-csv"]
    command = daff diff --git

[diff "perl"]
    xfuncname = "^\\s*(sub.*)"

[difftool]
    prompt = false

[init]
    defaultBranch = main

[fetch]
    prune = true

[gpg]
    format = ssh

[interactive]
    diffFilter = delta --color-only --features=interactive

[merge]
    conflictstyle = diff3
    tool = fugitive

[merge "daff-csv"]
    name   = daff tabular csv merge
    driver = daff merge --output %A %O %A %B

[mergetool]
    prompt = false

[mergetool "fugitive"]
    cmd = vim -f -c \"Gvdiffsplit!\" \"$MERGED\"

[push]
    default = current

[pull]
    rebase = false

[receive]
    denyCurrentBranch = warn

[rerere]
    enabled = true
    autoUpdate = true

[user]
    name = Chris Grau
    email = 113591+sirhc@users.noreply.github.com

# This needs to be last, to override anything above.
[include]
    path = ~/.gitconfig.local
